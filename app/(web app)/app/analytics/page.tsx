import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Analytics",
  };
const Analytics = () =>{
    return(
        <div>Analytics</div>
    )
}

export default Analytics